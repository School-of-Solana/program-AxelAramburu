/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  addDecoderSizePrefix,
  addEncoderSizePrefix,
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU32Decoder,
  getU32Encoder,
  getU64Decoder,
  getU64Encoder,
  getUtf8Decoder,
  getUtf8Encoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type Codec,
  type Decoder,
  type Encoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from '@solana/kit';
import { CROWDSOL_PROGRAM_ADDRESS } from '../programs';
import { getAccountMetaFactory, type ResolvedAccount } from '../shared';

export const DONATE_DISCRIMINATOR = new Uint8Array([
  121, 186, 218, 211, 73, 70, 196, 180,
]);

export function getDonateDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(DONATE_DISCRIMINATOR);
}

export type DonateInstruction<
  TProgram extends string = typeof CROWDSOL_PROGRAM_ADDRESS,
  TAccountParticipant extends string | AccountMeta<string> = string,
  TAccountParticipation extends string | AccountMeta<string> = string,
  TAccountVault extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends
    | string
    | AccountMeta<string> = '11111111111111111111111111111111',
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountParticipant extends string
        ? WritableSignerAccount<TAccountParticipant> &
            AccountSignerMeta<TAccountParticipant>
        : TAccountParticipant,
      TAccountParticipation extends string
        ? WritableAccount<TAccountParticipation>
        : TAccountParticipation,
      TAccountVault extends string
        ? WritableAccount<TAccountVault>
        : TAccountVault,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type DonateInstructionData = {
  discriminator: ReadonlyUint8Array;
  amount: bigint;
  comment: string;
};

export type DonateInstructionDataArgs = {
  amount: number | bigint;
  comment: string;
};

export function getDonateInstructionDataEncoder(): Encoder<DonateInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ['discriminator', fixEncoderSize(getBytesEncoder(), 8)],
      ['amount', getU64Encoder()],
      ['comment', addEncoderSizePrefix(getUtf8Encoder(), getU32Encoder())],
    ]),
    (value) => ({ ...value, discriminator: DONATE_DISCRIMINATOR })
  );
}

export function getDonateInstructionDataDecoder(): Decoder<DonateInstructionData> {
  return getStructDecoder([
    ['discriminator', fixDecoderSize(getBytesDecoder(), 8)],
    ['amount', getU64Decoder()],
    ['comment', addDecoderSizePrefix(getUtf8Decoder(), getU32Decoder())],
  ]);
}

export function getDonateInstructionDataCodec(): Codec<
  DonateInstructionDataArgs,
  DonateInstructionData
> {
  return combineCodec(
    getDonateInstructionDataEncoder(),
    getDonateInstructionDataDecoder()
  );
}

export type DonateInput<
  TAccountParticipant extends string = string,
  TAccountParticipation extends string = string,
  TAccountVault extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  participant: TransactionSigner<TAccountParticipant>;
  participation: Address<TAccountParticipation>;
  vault: Address<TAccountVault>;
  systemProgram?: Address<TAccountSystemProgram>;
  amount: DonateInstructionDataArgs['amount'];
  comment: DonateInstructionDataArgs['comment'];
};

export function getDonateInstruction<
  TAccountParticipant extends string,
  TAccountParticipation extends string,
  TAccountVault extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof CROWDSOL_PROGRAM_ADDRESS,
>(
  input: DonateInput<
    TAccountParticipant,
    TAccountParticipation,
    TAccountVault,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress }
): DonateInstruction<
  TProgramAddress,
  TAccountParticipant,
  TAccountParticipation,
  TAccountVault,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? CROWDSOL_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    participant: { value: input.participant ?? null, isWritable: true },
    participation: { value: input.participation ?? null, isWritable: true },
    vault: { value: input.vault ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      '11111111111111111111111111111111' as Address<'11111111111111111111111111111111'>;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.participant),
      getAccountMeta(accounts.participation),
      getAccountMeta(accounts.vault),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getDonateInstructionDataEncoder().encode(
      args as DonateInstructionDataArgs
    ),
    programAddress,
  } as DonateInstruction<
    TProgramAddress,
    TAccountParticipant,
    TAccountParticipation,
    TAccountVault,
    TAccountSystemProgram
  >);
}

export type ParsedDonateInstruction<
  TProgram extends string = typeof CROWDSOL_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    participant: TAccountMetas[0];
    participation: TAccountMetas[1];
    vault: TAccountMetas[2];
    systemProgram: TAccountMetas[3];
  };
  data: DonateInstructionData;
};

export function parseDonateInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>
): ParsedDonateInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 4) {
    // TODO: Coded error.
    throw new Error('Not enough accounts');
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      participant: getNextAccount(),
      participation: getNextAccount(),
      vault: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getDonateInstructionDataDecoder().decode(instruction.data),
  };
}
